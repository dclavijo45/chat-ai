services:
  app:
    build:
      context: .
    restart: unless-stopped
    container_name: chat-ai-frontend
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
